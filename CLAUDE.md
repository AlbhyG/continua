At the start of each session, run `git pull` to sync the latest changes (someone else may be developing against this repo too).

Commit and push after every change.

After pushing to origin main, run `vercel --prod` to deploy.
